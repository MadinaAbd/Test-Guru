<h2><p class="text-center"><%= t('.header') %></p></h2>
  <p>
    <%= link_to t('.new_test'), new_admin_test_path %>
  </p>

  <table class="table">
    <thead>
      <tr>
        <th><%= t('.title') %></th>
        <th><%= t('.level') %></th>
        <th><%= t('.questions') %></th>
        <th colspan="4"><%= t('.actions' %></th>
      </tr>
    </thead>

    <%= render @tests %>
  </table>
