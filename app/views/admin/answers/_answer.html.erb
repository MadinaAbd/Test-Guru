<li>
  <td><%= answer.title %></td>
  <td><%= link_to t('.show'), admin_answer_path(answer, class: 'btn btn-outline-primary') %></td>
  <td><%= link_to t('.edit'), edit_answer_path(answer), class: 'btn btn-outline-primary' %></td>
  <td><%= link_to t('.delete'),
                admin_answer_path(answer),
                class: 'btn btn-outline-primary'
                method: :delete,
                data: { confirm: t('.delete_confirm') } %></td>
</li>
