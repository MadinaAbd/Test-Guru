<li>
  <%= answer.title %>
  <td><%= link_to t('.show'), answer_path(answer) %></td>
  <td><%= link_to t('.edit'), edit_answer_path(answer) %></td>
  <td><%= link_to t('.delete'),
                answer_path(answer),
                method: :delete,
                data: { confirm: t('.delete_confirm') } %></td>
</li>
