<h>Прохождение <%= @test_passage.test.title %> теста </h>

<b>
  <%= @test_passage.current_questions.body %>
</b>
<p>
  <%= view_current_questions(@test_passage)%>
</p>
<%= form_with url: test_passage_path(@test_passage), local: true, method: :put do |form| %>
  <%= form.collection_check_boxes :answer_ids, @test_passage.current_questions.answers, :id, :title, include_hidden: false do |b| %>
    <p>
      <%= b.check_box id: "answer_ids_#{b.object.id}" %>
      <%= b.label %>
    </p>
  <% end %>

  <p><%= link_to 'Назад к тестам', tests_path%> </p>

  <p><%= form.submit 'Вперед' %></p>
  <% end %>
